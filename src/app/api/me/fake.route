// src/app/api/me/route.ts
import { NextResponse } from "next/server";

export async function GET() {
  // Retorna um usuário fake para testes
  const localUser = {
    id: "fake-user-123",
    nome: "Usuário Teste",
    email: "teste@exemplo.com",
    nivel_acesso: "admin_empresa",
    empresa_id: "empresa-xyz",
    empresa_nome: "Empresa XYZ",
    features: ["estoque", "financeiro", "dashboard"],
  };

  return NextResponse.json(localUser);
}